model User {
  id    Int      @id @default(autoincrement())

  email String   @unique

  // TODO: add name and profile img
  
  username  String @unique
  password String 

  pets Pet[]

  check_ins CheckIn[]

  created_at DateTime @default(now())

  deleted Boolean @default(false)
}

// TODO: add UserFavoritePark table