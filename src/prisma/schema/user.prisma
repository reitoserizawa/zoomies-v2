model User {
  id    Int      @id @default(autoincrement())

  email String   @unique

  // TODO: add name and profile img
  
  username  String @unique
  password String 

  pets Pet[]

  check_ins CheckIn[]

  favorites UserFavoritePark[]

  deleted Boolean @default(false)

  created_at DateTime @default(now())
}